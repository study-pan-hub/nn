# ============================================================
# Drone Vision Navigation System - Requirements
# 无人机视觉导航系统依赖配置文件
# ============================================================
# 包含模块：
#   - Web演示服务器 (Flask)
#   - AirSim仿真无人机控制
#   - TensorFlow/Keras深度学习模型
#   - PyTorch深度学习模型
#   - 计算机视觉处理
# ============================================================

# ---------- 核心深度学习框架 ----------
# PyTorch (用于 pytorch_model.py)
# 注意：请根据你的CUDA版本选择对应的torch版本
# CPU版本：pip install torch torchvision
# CUDA 11.8：pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# CUDA 12.1：pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0

# TensorFlow / Keras (用于 drone_nav_with_vision.py)
tensorflow>=2.13.0

# ---------- 计算机视觉 ----------
# OpenCV - 图像处理、视频流捕获
opencv-python>=4.8.0

# NumPy - 数值计算
numpy>=1.24.0

# Pillow - 图像读取和格式转换
Pillow>=10.0.0

# ---------- Web框架 ----------
# Flask - Web演示服务器 (app.py)
Flask>=2.3.0

# ---------- 无人机仿真 ----------
# AirSim - 微软无人机/汽车仿真器API
# 需要先安装AirSim仿真器，然后安装Python客户端
airsim>=1.8.0

# ---------- 可选/测试工具 ----------
# 用于调试和测试
pytest>=7.4.0

# ============================================================
# 安装说明
# ============================================================
# 1. 基础安装（全部依赖）：
#    pip install -r requirements.txt
#
# 2. 如果使用GPU（CUDA 11.8）安装PyTorch：
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# 3. AirSim安装说明：
#    - 下载AirSim仿真器：https://github.com/microsoft/AirSim/releases
#    - 运行AirSim仿真器后，Python客户端自动可用
#
# 4. 移动设备视频源（DroidCam）：
#    - 安装DroidCam手机应用
#    - 确保手机和电脑在同一Wi-Fi网络
#    - 在代码中更新VIDEO_SOURCE为手机IP
# ============================================================